{
  "name": "iwork-local",
  "version": "1.0.0",
  "description": "Aplicación local para guardar datos de usuarios",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install": "npm rebuild better-sqlite3 --build-from-source",
    "render-build": "npm install && npm rebuild better-sqlite3 --build-from-source"
  },
  "dependencies": {
    "better-sqlite3": "^11.9.1",
    "body-parser": "^1.20.3",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.2",
    "node-windows": "^1.0.0-beta.8",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": "18.x"
  }
}
